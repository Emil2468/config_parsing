from setuptools import find_packages, setup

setup(
    name='config_parsing',
    packages=find_packages(),
    version='0.1.0',
    description='Library for parsing json files to config classes',
    author='Mig og min ven Simon',
    license='MIT',
)
